.custom-home-smiles
  .custom-home-smiles__container
    .custom-home-smiles__title(v-html='props.title')
      
    SliderHomeSmiles(:props='props.gallery')
      
    //- .custom-home-smiles__slider
    //-   .custom-home-smiles__slide(v-for='slide in props.gallery')  
    //-     img(:src='slide.url')
